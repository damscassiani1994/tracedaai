__pycache__/
*.pyc
.venv/
venv/
data/*.db
data/*.db-journal
data/*.db.backup*
data/*.bak
*.egg-info/
.DS_Store
.vscode/
.idea/

# Machine-specific Claude Code hook wiring (hardcodes an absolute venv path) --
# generate your own locally with `tracedaai init-hooks`.
.claude/

# Same reason, for Cursor's native hooks -- generate locally with
# `tracedaai init-cursor-hooks`.
.cursor/
